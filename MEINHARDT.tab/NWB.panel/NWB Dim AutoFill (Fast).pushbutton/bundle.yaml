title:
  en_us: 'Dim\nAutoFill\n(Fast)'
tooltip:
  en_us: |
    Fill NWB_DimWidth / NWB_DimHeight / NWB_DimDiameter from the Size parameter
    for elements in the active schedule view — OPTIMIZED VERSION.

    Same features as standard version with significant speed improvements:
    - Deferred parameter verification (post-commit)
    - Parameter resolution caching
    - Faster verification workflow

    - Run from an open schedule view
    - Choose skip existing values or override existing values
    - Live progress monitor with cancel that keeps processed items
author: GM
engine:
  persistent: false